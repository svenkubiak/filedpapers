function showLoading(){let e=document.getElementById("loading-button");e&&(e.classList.add("is-loading"),e.disabled=!0)}document.addEventListener("DOMContentLoaded",()=>{let e=document.querySelector(".notification");if(e){let t=e.querySelector(".delete");t.addEventListener("click",()=>{e.remove()})}});const username=document.getElementById("username");username&&username.focus();